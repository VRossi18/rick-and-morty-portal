services:
  ollama:
    container_name: rick-morty-ollama
    image: docker.io/ollama/ollama:latest
    ports:
      - '11434:11434'
    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data:
